# Optional direct backend URL for local testing or bypassing the Vite proxy.
# VITE_API_URL=http://localhost:5000/api

# Required on Vercel if you want /api requests to be proxied to Render.
# BACKEND_URL=https://your-backend-name.onrender.com
